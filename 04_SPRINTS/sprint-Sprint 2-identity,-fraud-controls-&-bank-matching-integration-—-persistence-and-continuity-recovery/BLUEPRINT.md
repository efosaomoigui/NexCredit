1) Map all borrower checkpoints and backend states, then define a single authoritative state machine contract. 2) Implement/patch offer acceptance API for idempotency, atomic persistence, and explicit error envelopes. 3) Update mobile acceptance flow to use request IDs, retry-safe logic, and backend-confirmed success transitions. 4) Reconcile stage calculator/rendering to backend state source. 5) Harden resume logic for app restart and returning users with unpaid-loan guard. 6) Add fallback status metadata and environment gating for provider-unavailable paths. 7) Add critical-path automated tests and manual E2E checklist with trace IDs.