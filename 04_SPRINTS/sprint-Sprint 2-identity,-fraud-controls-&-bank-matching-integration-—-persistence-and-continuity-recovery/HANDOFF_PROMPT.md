Continue Sprint 2 only. Do not refactor outside scope. Start by tracing first-offer acceptance from mobile action to DB write and admin read model. Implement idempotent acceptance with atomic persistence and explicit retryable error contract. Then align stage progression to backend-authoritative states and repair resume drift for app restart/returning users. Preserve fallback envelopes for unavailable providers with environment gating and status metadata. Verify with critical-path tests and manual E2E before handoff.