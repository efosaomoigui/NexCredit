A) New user can complete onboarding and accept first loan offer end-to-end with record persisted in database and visible in admin. B) Returning applicant can complete flow without dead-end or blocked transition. C) On network failure during acceptance, user receives explicit recoverable error and retry succeeds without duplicate records. D) Stage calculation and displayed progress match backend workflow state in all tested paths. E) Existing validated UI structure remains unchanged except required bug fixes.