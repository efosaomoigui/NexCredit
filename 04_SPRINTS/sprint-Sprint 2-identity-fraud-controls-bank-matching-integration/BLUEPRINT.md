Backend: add/verify idempotent acceptance endpoint behavior, persistence guarantees, request/response contract, and admin-query readiness. Mobile: enforce request lifecycle states (loading/success/failure), offline/network error messaging, retry action, and post-success navigation guards. Shared state: define canonical loan stage enum and transition mapping used by API + React Native. Admin: expose accepted submissions in queue/list with minimum identifying metadata and status. QA: add integration tests for first acceptance submission, network-failure retry, stage progression correctness, and returning-user completion.